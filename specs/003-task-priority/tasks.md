# Задачи: Приоритет задачи (редактируемый, сортировка, перетаскивание в группе)

**Ввод**: Документы из `specs/003-task-priority/` (plan.md, spec.md, data-model.md, contracts/)  
**Предусловия**: plan.md, spec.md, research.md, data-model.md, contracts/task-priority-types.md

**Организация**: Задачи сгруппированы по пользовательским историям; каждую историю можно реализовать и проверить отдельно.

## Формат: `[ID] [P?] [Story] Описание с путём к файлу`

- **[P]**: Можно выполнять параллельно (другие файлы, нет зависимостей от незавершённых задач)
- **[Story]**: К какой истории относится (US1, US2, US3)
- В описании указывать точные пути к файлам

## Соглашения по путям

- Один проект: `src/`, `tests/` в корне репозитория (согласно plan.md).

---

## Phase 1: Setup (общая инфраструктура)

**Цель**: Типы и константы приоритета, необходимые для store и UI.

- [x] T001 [P] Добавить тип `TaskPriority` и расширить интерфейс `Task` полем `priority` в src/types/task.ts; добавить константы: порядок уровней (High, Medium, Low), приоритет по умолчанию (Medium), маппинг приоритета на CSS-класс или цвет (по contracts/task-priority-types.md и data-model.md).

---

## Phase 2: Основа (блокирующие предварительные условия)

**Цель**: Хранилище и API приоритета; без этой фазы нельзя реализовывать пользовательские истории.

**⚠️ КРИТИЧНО**: К пользовательским историям приступать только после завершения этой фазы.

- [x] T002 Обновить src/stores/todoStore.ts: при загрузке из localStorage нормализовать поле priority (отсутствует или невалидно → 'Medium'); в addTask задавать priority по умолчанию; добавить action setPriority(id, priority) с перемещением задачи в конец новой группы (пересчёт order); добавить геттеры для групп по приоритету (например tasksByPriorityHigh, tasksByPriorityMedium, tasksByPriorityLow или один sortedTasks: сначала по приоритету High→Medium→Low, затем по order); добавить action reorderTasksInGroup(priority, fromIndex, toIndex) для перетаскивания внутри группы; сохранять в localStorage после каждой мутации (по contracts/task-priority-types.md и research.md).

**Контрольная точка**: Основа готова — можно приступать к US1, US2, US3.

---

## Phase 3: User Story 1 — Отображение и редактирование приоритета по клику (Приоритет: P1) 🎯 MVP

**Цель**: Пользователь видит приоритет задачи и может изменить его по клику через выпадающий список (Low, Medium, High); изменение сохраняется.

**Независимая проверка**: Добавить задачу → кликнуть по приоритету → выбрать другое значение → приоритет изменился; перезагрузить страницу → приоритет сохранён.

- [x] T003 [P] [US1] В src/components/TaskItem.vue отобразить текущий приоритет задачи (текст или бейдж); по клику показывать лёгкий выпадающий список (select или кастомный dropdown) с вариантами Low, Medium, High; при выборе вызывать store.setPriority(task.id, value) и закрывать список (по spec FR-002 и research.md).
- [x] T004 [US1] В src/components/TaskItem.vue при отмене редактирования (клик вне, Escape) не вызывать setPriority — сохранять предыдущее значение приоритета (по граничным случаям spec).
- [x] T005 [US1] В src/components/TaskItem.vue задать еле уловимый цвет карточки по приоритету: High — красный, Medium — жёлтый, Low — синий (CSS-классы .priority-high, .priority-medium, .priority-low или инлайн-стили по data-model.md).

**Контрольная точка**: US1 можно проверить отдельно — приоритет отображается, меняется по клику и сохраняется после перезагрузки.

---

## Phase 4: User Story 2 — Сортировка списка по приоритету (Приоритет: P1)

**Цель**: Список отображается в порядке High → Medium → Low; внутри группы — по order; карточки окрашены по приоритету (уже в US1).

**Независимая проверка**: Иметь задачи с разными приоритетами → в списке сначала High, затем Medium, затем Low; изменить приоритет задачи → она переместилась в конец новой группы.

- [ ] T006 [US2] В src/components/TaskList.vue (и при необходимости src/views/HomeView.vue) рендерить задачи по группам приоритета: три секции (High, Medium, Low), в каждой — задачи с соответствующим приоритетом, упорядоченные по order; использовать геттеры store (tasksByPriorityHigh и т.п. или sortedTasks с группировкой по data-model.md и FR-003).

**Контрольная точка**: US2 выполнена — список отсортирован по приоритету, группы видны.

---

## Phase 5: User Story 3 — Перетаскивание только в рамках одного приоритета (Приоритет: P2)

**Цель**: Перетаскивание меняет порядок только внутри одной группы приоритета; drop в зоне другой группы недоступен.

**Независимая проверка**: Перетащить задачу Low среди задач Low — порядок изменился; попытаться перетащить задачу Low в зону Medium — drop не срабатывает.

- [ ] T007 [US3] В src/components/TaskList.vue для каждой группы приоритета использовать отдельный экземпляр VueDraggableNext (v-model на список задач этой группы); при @end вызывать store.reorderTasksInGroup(priority, oldIndex, newIndex); убедиться, что drop между группами физически невозможен (по research.md и FR-004).

**Контрольная точка**: US3 выполнена — перетаскивание только внутри группы, порядок сохраняется после перезагрузки.

---

## Phase 6: Polish и сквозные задачи

**Цель**: Проверка по quickstart и актуализация документации.

- [ ] T008 Пройти проверку по сценариям из specs/003-task-priority/quickstart.md (запуск dev, добавление/смена приоритета, сортировка, перетаскивание в группе, перезагрузка).
- [ ] T009 [P] При необходимости обновить README в корне: упомянуть приоритеты задач (Low, Medium, High) и перетаскивание в рамках приоритета.

---

## Зависимости и порядок выполнения

### Зависимости фаз

- **Phase 1 (Setup)**: Без зависимостей — можно начинать сразу.
- **Phase 2 (Основа)**: Зависит от Phase 1 — блокирует все пользовательские истории.
- **Phase 3 (US1)**: Зависит от Phase 2; T003, T005 можно помечать [P] внутри фазы; T004 после T003.
- **Phase 4 (US2)**: Зависит от Phase 2; использует геттеры из T002; визуально опирается на T005 (цвет уже в TaskItem).
- **Phase 5 (US3)**: Зависит от Phase 2 и Phase 4 (рендер по группам должен быть готов); одна задача T007 в TaskList.
- **Phase 6 (Polish)**: Зависит от завершения Phase 3–5.

### Пользовательские истории

- **US1**: После T002 — отображение и смена приоритета в TaskItem.
- **US2**: После T002 — сортировка и группы в TaskList; не зависит от US1 логически, но UI приоритета (TaskItem) удобнее сделать в US1.
- **US3**: После T002 и структуры групп (US2) — перетаскивание по группе в TaskList.

### Параллельные возможности

- T001 — один файл, можно раньше T002.
- T003 и T005 в Phase 3 — разные части TaskItem, можно параллелить; T004 после T003.
- T009 — параллельно с T008.

---

## Стратегия реализации

### MVP сначала (User Story 1)

1. Выполнить Phase 1 и Phase 2.
2. Выполнить Phase 3 (TaskItem: приоритет, dropdown, цвет).
3. Остановиться и проверить: приоритет меняется по клику и сохраняется после перезагрузки.
4. Затем Phase 4 (сортировка по группам), Phase 5 (drag в группе), Phase 6.

### Пошаговая поставка

1. Phase 1 → Phase 2 → основа готова.
2. US1 (Phase 3) → проверить отдельно → MVP приоритета.
3. US2 (Phase 4) → проверить сортировку и группы.
4. US3 (Phase 5) → проверить перетаскивание только в группе.
5. Phase 6 → quickstart и README.

---

## Заметки

- [P] у задач: разные файлы или независимые части одного файла.
- [Story] связывает задачу с пользовательской историей из spec.md для прослеживаемости.
- Перетаскивание: три экземпляра vue-draggable-next (по одному на группу) обеспечивают «drop не срабатывает» вне своей группы (research.md).
- Цвет карточки: еле уловимые оттенки (пастельные) по data-model.md; конкретные значения — в реализации (CSS-переменные или классы).
