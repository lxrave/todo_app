# Задачи: Приложение для управления задачами (Todo List)

**Ввод**: Документы из `specs/001-todo-list/` (plan.md, spec.md, data-model.md, contracts/)  
**Предусловия**: plan.md, spec.md

**Организация**: Задачи сгруппированы по пользовательским историям для независимой реализации и проверки.

## Формат: `[ID] [P?] [Story] Описание с путём к файлу`

- **[P]**: Можно выполнять параллельно (другие файлы, нет зависимостей от незавершённых задач)
- **[Story]**: К какой истории относится (US1, US2, US3, US4)
- В описании указывать точные пути к файлам

## Соглашения по путям

- Один проект: `src/`, `tests/` в корне репозитория (согласно plan.md).

---

## Phase 1: Setup (общая инфраструктура)

**Цель**: Инициализация проекта и базовая структура

- [x] T001 Создать структуру каталогов по плану: src/components, src/views, src/stores, src/types, src/composables, tests/unit, public — в корне репозитория
- [x] T002 Инициализировать проект Vite + Vue 3 + TypeScript с зависимостями Pinia (package.json, vite.config.ts, tsconfig, main.ts, базовый App.vue и index.html) в корне репозитория
- [x] T003 [P] Настроить линтинг и форматирование (ESLint, Prettier) в корне репозитория

---

## Phase 2: Основа (блокирующие предусловия)

**Цель**: Инфраструктура, без которой нельзя приступать ни к одной пользовательской истории

**⚠️ КРИТИЧНО**: К пользовательским историям приступать только после завершения этой фазы

- [x] T004 [P] Описать интерфейс Task в src/types/task.ts (id, text, completed, order) по data-model.md и contracts/task-types.md
- [x] T005 Реализовать Pinia-хранилище в src/stores/todoStore.ts: state tasks, loadTasks() из localStorage, сохранение после мутаций, addTask(text), toggleTask(id), reorderTasks(fromIndex, toIndex); константа ключа localStorage; при невалидном JSON — пустой массив

**Контрольная точка**: Основа готова — можно приступать к пользовательским историям

---

## Phase 3: User Story 1 — Добавление задачи (Приоритет: P1) 🎯 MVP

**Цель**: Пользователь вводит текст и добавляет задачу; она появляется в списке и сохраняется.

**Независимая проверка**: Открыть приложение → ввести текст → нажать «Добавить» → задача отображается в списке; после перезагрузки страницы задача остаётся.

- [x] T006 [P] [US1] Реализовать компонент формы добавления задачи в src/components/TaskForm.vue (поле ввода, кнопка «Добавить»; вызов store.addTask при отправке)
- [x] T007 [US1] Добавить валидацию пустого ввода в src/components/TaskForm.vue: кнопка неактивна при пустом/только пробелы тексте; не вызывать addTask с пустой строкой (FR-006)
- [x] T008 [US1] Реализовать главный экран в src/views/HomeView.vue: TaskForm и отображение списка задач из store; подключить HomeView в App.vue

**Контрольная точка**: US1 можно проверить отдельно: добавление и отображение в списке работают

---

## Phase 4: User Story 2 — Просмотр списка задач (Приоритет: P2)

**Цель**: Пользователь видит все задачи (текст и статус); при пустом списке — явное пустое состояние.

**Независимая проверка**: Добавить несколько задач → убедиться, что все отображаются с текстом и статусом; очистить список → увидеть пустое состояние без ошибок.

- [x] T009 [P] [US2] Реализовать компонент списка задач в src/components/TaskList.vue: отображение текста и статуса каждой задачи; пустое состояние при пустом списке (сообщение или заглушка)
- [x] T010 [US2] Подключить TaskList в src/views/HomeView.vue, передавать список из store; заменить временный список на TaskList

**Контрольная точка**: US2 выполнена — список и пустое состояние отображаются корректно

---

## Phase 5: User Story 3 — Отметка задачи как выполненной (Приоритет: P3)

**Цель**: Пользователь переключает статус задачи (выполнена / не выполнена); изменение отображается и сохраняется.

**Независимая проверка**: Добавить задачу → переключить статус → убедиться, что отображение изменилось и после перезагрузки состояние сохранилось.

- [x] T011 [P] [US3] Реализовать компонент одного элемента задачи в src/components/TaskItem.vue: отображение text и индикатор completed; клик/тап по элементу или чекбоксу для переключения
- [x] T012 [US3] Подключить в src/components/TaskItem.vue вызов store.toggleTask(id) при переключении статуса
- [x] T013 [US3] Использовать TaskItem внутри src/components/TaskList.vue для каждой задачи; убедиться, что переключение сохраняется после перезагрузки (FR-004)

**Контрольная точка**: US3 выполнена — переключение статуса работает и персистится

---

## Phase 6: User Story 4 — Переупорядочивание перетаскиванием (Приоритет: P4)

**Цель**: Пользователь меняет порядок задач перетаскиванием (мышь или тач); новый порядок сохраняется. Список остаётся реактивным при добавлении новых задач (источник правды — store.tasks).

**Независимая проверка**: Иметь минимум две задачи → перетащить одну → порядок изменился и после перезагрузки сохранился. Добавить новую задачу → она сразу появляется в списке.

- [x] T014 [US4] Добавить библиотеку перетаскивания vue-draggable-next (и sortablejs) в package.json; реализовать в src/stores/todoStore.ts логику reorderTasks(fromIndex, toIndex): пересчёт order и сохранение в localStorage
- [x] T015 [US4] Сделать список в src/components/TaskList.vue перетаскиваемым: ref list, синхронизация с store.tasks через watch(store.tasks, …, { deep: true, flush: 'sync' }); VueDraggableNext :list="list"; при @end вызывать store.reorderTasks (FR-005, план: перетаскивание не ломает отображение новых задач)

**Контрольная точка**: US4 выполнена — перетаскивание меняет порядок и сохраняет его; добавление задач по-прежнему отображается

---

## Phase 7: Polish и сквозные задачи

**Цель**: Тесты, документация и проверка по quickstart

- [x] T016 [P] Настроить Vitest и @vue/test-utils; добавить юнит-тест хранилища в tests/unit/stores/todoStore.spec.ts (addTask, toggleTask, load/persist)
- [x] T017 [P] Добавить юнит-тест одного из компонентов (TaskForm или TaskItem) в tests/unit/components/
- [x] T018 Пройти проверку по сценариям из specs/001-todo-list/quickstart.md (запуск dev, build, test; ручная проверка US1–US4 и FR-006)
- [x] T019 [P] Обновить README в корне: команды npm run dev, build, test и краткое описание проекта

---

## Зависимости и порядок выполнения

### Зависимости фаз

- **Setup (Phase 1)**: Без зависимостей — можно начинать сразу
- **Основа (Phase 2)**: Зависит от Setup — блокирует все пользовательские истории
- **User Stories (Phase 3–6)**: Зависят от завершения Phase 2; между собой US1→US2→US3→US4 по порядку
- **Polish (Phase 7)**: Зависит от завершения нужных пользовательских историй

### Внутри фаз

- Phase 2: T004 и T005 (T005 зависит от T004 — типы для store)
- Phase 3: T006 можно параллельно с T008; T007 после T006; T008 после T005
- Phase 4: T009 затем T010
- Phase 5: T011, T012, T013 по порядку
- Phase 6: T014 затем T015
- Phase 7: T016, T017, T019 — параллельно; T018 — после реализации

### Параллельные возможности

- T003, T004 помечены [P] в своих фазах
- T006, T009, T011 — [P]; T016, T017, T019 — [P]

---

## Стратегия реализации

### MVP сначала (только User Story 1)

1. Выполнить Phase 1: Setup  
2. Выполнить Phase 2: Основа  
3. Выполнить Phase 3: User Story 1  
4. **Остановиться и проверить**: добавление и отображение задачи, сохранение после перезагрузки  
5. При необходимости — демо/деплой

### Пошаговая поставка

1. Setup + Основа → можно переходить к историям  
2. US1 → проверить отдельно → MVP  
3. US2 → проверить список и пустое состояние  
4. US3 → проверить переключение статуса  
5. US4 → проверить перетаскивание и что новые задачи по-прежнему появляются  
6. Polish → тесты и quickstart

---

## Заметки

- [P] = задачи в разных файлах без взаимных зависимостей
- Метка [USn] связывает задачу с историей для трассировки
- Каждую историю желательно завершать и проверять отдельно
- Коммитить после каждой задачи или логической группы
- US4: список для draggable синхронизировать с store.tasks (watch deep + flush: 'sync'), чтобы добавление задач не ломало отображение (см. plan.md и research.md)
